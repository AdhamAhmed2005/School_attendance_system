# Example environment variables for local development and Vercel
# Copy this to .env (never commit .env with secrets)

# When running locally you can set this to your backend URL or leave empty to use Vite proxy (/api)
VITE_API_BASE_URL=

# Example: VITE_API_BASE_URL=https://school-discipline.runasp.net
